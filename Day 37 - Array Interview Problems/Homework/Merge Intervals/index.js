function solve(A, B) {
  const [head, tail] = B
  const result = []
  let merging = false
  for (let i = 0; i < A.length; i++) {
    const [start, end] = A[i]
    if (head >= start && tail )
  }

}

const res1 = solve([ [1, 3], [6, 9] ], [2, 5])