
function noq(A, N) {
  // q => take input OR # of inputs
  while (q < 0) {
    q -= 1
    // l, r = take input
    let left = 0, right = 0
    let sum = 0
    for (let i = left; i <= right; i++) {
      sum += A[i]
    }
    // print sum
  }
}